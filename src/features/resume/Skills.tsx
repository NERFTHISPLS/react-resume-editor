import AddBlockButton from '../../ui/AddBlockButton';

export default function Skills() {
  return (
    <section className="flex flex-col">
      <AddBlockButton>+ Добавить навыки</AddBlockButton>
    </section>
  );
}
